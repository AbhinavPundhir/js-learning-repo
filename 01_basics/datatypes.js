"use strict"; //treat all js code as newer version.

//alert("hello")// we are running on nodejs not browser

console.log("Abhinav")

console.log(3+3)

//null -> a standalone value in js. This has a type "object" instead of a special type defined for it.

//undefined -> when we don't mention the value for a particular data type. This has a type "undefined" specially for it.
